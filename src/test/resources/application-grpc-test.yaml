spring:
  config:
    activate:
      on-profile: grpc-test
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false
      fail-fast: false
  jpa:
    hibernate:
      ddl-auto: create-drop
  grpc:
    server:
      port: 9099
      reflection:
        enabled: true
eureka:
  client:
    enabled: false

loki:
  enabled: false
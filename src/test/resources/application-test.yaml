# application-test.yml
spring:
  config:
    activate:
      on-profile: test
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false
      fail-fast: false
  jpa:
    hibernate:
      ddl-auto: create-drop
  grpc:
    server:
      port: 0
      reflection:
        enabled: true
eureka:
  client:
    enabled: false

nats:
  enabled: false